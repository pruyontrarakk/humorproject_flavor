# Supabase (browser + server)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Caption chain API — full URL your course expects (POST JSON).
# The app sends: { image_url, humor_flavor_id, steps: [...] }
CAPTION_API_URL=https://api.almostcrackd.ai/your/path/here

# Optional Bearer token if the API requires it
CAPTION_API_KEY=

# Aliases (optional — same meaning as above)
# ALMOST_CRACKD_CAPTION_URL=
# ALMOST_CRACKD_API_KEY=
